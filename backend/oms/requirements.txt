# 🎯 OMS Service Dependencies - DUPLICATE DEPENDENCIES RESOLVED!
#
# ❌ 이전: 개별 requirements에서 라이브러리 버전을 중복 관리 → 버전 불일치 위험
# ✅ 현재: shared/pyproject.toml에서 중앙집중 관리 → 단일 진실 공급원
#
# 현재 shared 패키지에서 제공하는 의존성:
# - fastapi==0.115.0: API 프레임워크
# - asyncpg==0.29.0: PostgreSQL 비동기 드라이버
# - redis[hiredis]==5.0.1: 캐싱 및 세션 관리
# - python-jose[cryptography]==3.3.0: JWT 인증
# - passlib[bcrypt]==1.7.4 + bcrypt==4.1.1: 패스워드 해싱
# - 기타 모든 공통 라이브러리 (pydantic, httpx, uvicorn 등)

# 🔗 Shared package - 모든 의존성의 단일 진실 공급원
-e ../shared

# 📝 개발/테스트 의존성은 shared의 optional-dependencies 사용
# 사용법: pip install -e ../shared[dev,test]
