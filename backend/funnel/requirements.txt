# 🎯 Funnel Service Dependencies - DUPLICATE DEPENDENCIES RESOLVED!
#
# ❌ 이전: 모든 라이브러리를 개별적으로 관리 → 버전 불일치 및 중복 관리 오버헤드
# ✅ 현재: shared/pyproject.toml에서 중앙집중 관리 → 단일 진실 공급원
#
# 현재 shared 패키지에서 제공하는 의존성:
# - fastapi==0.115.0 + uvicorn==0.31.1: API 프레임워크 & 서버
# - pandas==2.1.3 + numpy==1.24.3: 데이터 분석 및 타입 추론
# - email-validator==2.0.0 + phonenumbers==8.13.0: 데이터 검증
# - google-auth, google-api-python-client 등: Google Sheets 지원
# - openpyxl==3.1.2: Excel 파일 처리
# - python-dotenv, python-json-logger: 유틸리티
# - 기타 모든 공통 라이브러리

# 🔗 Shared package - 모든 의존성의 단일 진실 공급원
-e ../shared

# 📝 개발/테스트 의존성은 shared의 optional-dependencies 사용
# 사용법: pip install -e ../shared[dev,test]