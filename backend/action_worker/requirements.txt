# Action Worker Service Dependencies
#
# Shared package provides all runtime dependencies:
# - confluent-kafka (Kafka consumer)
# - asyncpg (Postgres ActionLog registry)
# - boto3/aioboto3 (S3/MinIO + lakeFS S3 gateway)
# - httpx (OMS/resource service calls)
#
# Keep service-specific requirements empty unless action_worker adds unique deps.
-e ../shared
