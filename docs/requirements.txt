# Convenience requirements for building the Sphinx site locally.
#
# Usage:
#   python3.11 -m venv .venv-docs
#   source .venv-docs/bin/activate
#   pip install -r docs/requirements.txt
#   make -C docs html

# Build backend for editable installs (venv may not ship setuptools/wheel by default).
setuptools>=61.0
wheel

# Install the shared package + docs extras from the repo root.
-e backend/shared[docs]
