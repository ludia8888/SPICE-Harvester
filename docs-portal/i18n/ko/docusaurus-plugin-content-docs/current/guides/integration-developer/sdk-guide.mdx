---
title: SDK 가이드
sidebar_position: 3
---

# SDK 가이드(SDK Guide)

이 가이드는 통합 개발자(Integration Developer)에게 공식 Spice OS 클라이언트 SDK 사용법을 안내합니다. SDK는 Python 및 TypeScript용 REST API에 대한 타입 안전(Type-safe)하고 인체공학적인(Ergonomic) 래퍼를 제공합니다.

## 설치(Installation)

### Python SDK

```bash
pip install spice-harvester-sdk
```

### TypeScript SDK

```bash
npm install @spice-harvester/sdk
```

## 클라이언트 구성(Client Configuration)

### Python

```python
from spice_harvester import SpiceClient

client = SpiceClient(
    base_url="http://localhost:8080/api",
    token="your-access-token",
    timeout=30,           # 요청 타임아웃 (초)
    max_retries=3,        # 일시적 오류에 대한 재시도 횟수
)
```

### TypeScript

```typescript
import { SpiceClient } from '@spice-harvester/sdk';

const client = new SpiceClient({
  baseUrl: 'http://localhost:8080/api',
  token: 'your-access-token',
  timeout: 30000,       // 요청 타임아웃 (밀리초)
  maxRetries: 3,        // 일시적 오류에 대한 재시도 횟수
});
```

### 구성 옵션(Configuration Options)

| 옵션 | Python | TypeScript | 설명 |
|--------|--------|-----------|-------------|
| 기본 URL | `base_url` | `baseUrl` | BFF 서비스 URL |
| 토큰 | `token` | `token` | Bearer 인증 토큰 |
| 타임아웃 | `timeout` | `timeout` | 요청 타임아웃 |
| 재시도 | `max_retries` | `maxRetries` | 5xx/429 오류에 대한 자동 재시도 횟수 |
| 헤더 | `extra_headers` | `extraHeaders` | 추가 HTTP 헤더 |

## 객체 작업(Object Operations)

### 객체 목록 조회(List Objects)

**Python:**

```python
# 페이지네이션을 포함한 전체 직원 목록 조회
employees = client.objects.list(
    ontology="acme",
    object_type="Employee",
    page_size=25
)

for employee in employees:
    print(f"{employee.properties['fullName']} - {employee.properties['department']}")
```

**TypeScript:**

```typescript
const employees = await client.objects.list('acme', 'Employee', {
  pageSize: 25,
});

for (const emp of employees.data) {
  console.log(`${emp.properties.fullName} - ${emp.properties.department}`);
}
```

### 단일 객체 조회(Get Single Object)

**Python:**

```python
employee = client.objects.get(
    ontology="acme",
    object_type="Employee",
    primary_key="EMP-042"
)
print(employee.properties["fullName"])  # "Jane Doe"
```

**TypeScript:**

```typescript
const employee = await client.objects.get('acme', 'Employee', 'EMP-042');
console.log(employee.properties.fullName); // "Jane Doe"
```

### 객체 타입 목록 조회(List Object Types)

**Python:**

```python
object_types = client.ontology.list_object_types(ontology="acme")
for ot in object_types:
    print(f"{ot.api_name}: {len(ot.properties)} properties")
```

**TypeScript:**

```typescript
const objectTypes = await client.ontology.listObjectTypes('acme');
for (const ot of objectTypes.data) {
  console.log(`${ot.apiName}: ${Object.keys(ot.properties).length} properties`);
}
```

## 검색(Search)

### 기본 검색(Basic Search)

**Python:**

```python
results = client.objects.search(
    ontology="acme",
    object_type="Employee",
    where={
        "type": "eq",
        "field": "department",
        "value": "Engineering"
    },
    order_by=[{"field": "fullName", "direction": "asc"}],
    page_size=50
)

for emp in results:
    print(emp.properties["fullName"])
```

**TypeScript:**

```typescript
const results = await client.objects.search('acme', 'Employee', {
  where: {
    type: 'eq',
    field: 'department',
    value: 'Engineering',
  },
  orderBy: { fields: [{ field: 'fullName', direction: 'asc' }] },
  pageSize: 50,
});
```

### 복합 검색 쿼리(Complex Search Queries)

**Python:**

```python
# 연봉 10만 이상인 시니어 엔지니어
results = client.objects.search(
    ontology="acme",
    object_type="Employee",
    where={
        "type": "and",
        "value": [
            {"type": "eq", "field": "department", "value": "Engineering"},
            {"type": "gte", "field": "salary", "value": 100000},
            {
                "type": "or",
                "value": [
                    {"type": "eq", "field": "level", "value": "Senior"},
                    {"type": "eq", "field": "level", "value": "Staff"},
                ]
            }
        ]
    },
    select=["employeeId", "fullName", "salary", "level"]
)
```

### 텍스트 검색(Text Search)

**Python:**

```python
# 자동 완성(Typeahead / Autocomplete)
results = client.objects.search(
    ontology="acme",
    object_type="Employee",
    where={
        "type": "containsAllTermsInOrderPrefixLastTerm",
        "field": "fullName",
        "value": "jane d"
    },
    page_size=10
)
```

## 액션(Actions)

### 객체 생성(Create Object)

**Python:**

```python
result = client.actions.apply(
    ontology="acme",
    action_type="createObject",
    parameters={
        "objectType": "Employee",
        "properties": {
            "employeeId": "EMP-099",
            "fullName": "John Smith",
            "department": "Marketing",
            "startDate": "2024-01-15"
        }
    }
)
```

**TypeScript:**

```typescript
const result = await client.actions.apply('acme', 'createObject', {
  parameters: {
    objectType: 'Employee',
    properties: {
      employeeId: 'EMP-099',
      fullName: 'John Smith',
      department: 'Marketing',
      startDate: '2024-01-15',
    },
  },
});
```

### 객체 수정(Edit Object)

**Python:**

```python
client.actions.apply(
    ontology="acme",
    action_type="editObject",
    parameters={
        "objectType": "Employee",
        "primaryKey": "EMP-099",
        "properties": {
            "department": "Sales"
        }
    }
)
```

### 객체 삭제(Delete Object)

**Python:**

```python
client.actions.apply(
    ontology="acme",
    action_type="deleteObject",
    parameters={
        "objectType": "Employee",
        "primaryKey": "EMP-099"
    }
)
```

### 배치 액션(Batch Actions)

**Python:**

```python
results = client.actions.apply_batch(
    ontology="acme",
    action_type="createObject",
    requests=[
        {"parameters": {"objectType": "Employee", "properties": {"employeeId": "EMP-100", "fullName": "Alice"}}},
        {"parameters": {"objectType": "Employee", "properties": {"employeeId": "EMP-101", "fullName": "Bob"}}},
    ]
)
```

### 액션 시뮬레이션(Simulate Action)

액션을 적용하지 않고 테스트합니다.

**Python:**

```python
simulation = client.actions.simulate(
    ontology="acme",
    action_type="editObject",
    parameters={
        "objectType": "Employee",
        "primaryKey": "EMP-042",
        "properties": {"department": "Research"}
    }
)
print(simulation.validation_result)  # 액션 성공 여부 확인
```

## 페이지네이션(Pagination)

### 자동 페이지네이션(Automatic Pagination)

SDK는 반복(Iteration) 시 자동으로 페이지네이션을 처리합니다.

**Python:**

```python
# 모든 페이지를 자동으로 반복
for employee in client.objects.list("acme", "Employee", page_size=100):
    process(employee)
```

### 수동 페이지네이션(Manual Pagination)

더 세밀한 제어가 필요한 경우, 페이지 토큰을 직접 사용합니다.

**Python:**

```python
page = client.objects.list_page(
    ontology="acme",
    object_type="Employee",
    page_size=25
)

while page:
    for employee in page.data:
        process(employee)

    if page.next_page_token:
        page = client.objects.list_page(
            ontology="acme",
            object_type="Employee",
            page_size=25,
            page_token=page.next_page_token
        )
    else:
        break
```

## 오류 처리(Error Handling)

### Python

```python
from spice_harvester.exceptions import (
    SpiceApiError,
    ObjectNotFoundError,
    PermissionDeniedError,
    RateLimitError,
)

try:
    employee = client.objects.get("acme", "Employee", "EMP-999")
except ObjectNotFoundError as e:
    print(f"Employee not found: {e.primary_key}")
except PermissionDeniedError:
    print("Insufficient permissions")
except RateLimitError as e:
    print(f"Rate limited. Retry after {e.retry_after} seconds")
except SpiceApiError as e:
    print(f"API error: {e.error_code} - {e.message}")
```

### TypeScript

```typescript
import { SpiceApiError, ObjectNotFoundError } from '@spice-harvester/sdk';

try {
  const employee = await client.objects.get('acme', 'Employee', 'EMP-999');
} catch (error) {
  if (error instanceof ObjectNotFoundError) {
    console.log(`Employee not found: ${error.primaryKey}`);
  } else if (error instanceof SpiceApiError) {
    console.log(`API error: ${error.errorCode} - ${error.message}`);
  }
}
```

## 로깅 및 디버깅(Logging and Debugging)

### 요청 로깅 활성화(Enable Request Logging)

**Python:**

```python
import logging
logging.basicConfig(level=logging.DEBUG)

client = SpiceClient(
    base_url="http://localhost:8080/api",
    token="your-token",
    debug=True  # 모든 요청 및 응답 로깅
)
```

**TypeScript:**

```typescript
const client = new SpiceClient({
  baseUrl: 'http://localhost:8080/api',
  token: 'your-token',
  debug: true, // 모든 요청 및 응답 로깅
});
```

## SDK vs REST: 각각의 사용 시기

| 시나리오 | 권장 | 이유 |
|----------|---------------|-----|
| 애플리케이션 개발 | **SDK** | 타입 안전성, 자동 페이지네이션, 오류 처리 |
| 빠른 스크립팅 | **SDK** | 보일러플레이트 코드 감소 |
| 셸 스크립트 / cURL | **REST** | 셸에서 SDK 사용 불가 |
| 커스텀 HTTP 클라이언트 | **REST** | HTTP 계층에 대한 완전한 제어 |
| 지원되지 않는 언어 | **REST** | SDK는 Python 및 TypeScript에서만 사용 가능 |
| API 문제 디버깅 | **REST** | 원시(Raw) 요청/응답 검사가 더 용이 |

## 사전 요구 사항(Prerequisites)

- [API 개요](/docs/api/overview) -- SDK가 기반으로 하는 API 규칙
- [REST API 가이드](./rest-api) -- 기반이 되는 REST API 이해

## 다음 단계(Next Steps)

- **[인증 흐름 가이드](./auth-flow)** -- SDK 클라이언트에서의 토큰 관리
- **[v2 검색 API](/docs/api/v2-search)** -- 검색 연산자 레퍼런스
- **[REST API 가이드](./rest-api)** -- 직접 REST API 사용
